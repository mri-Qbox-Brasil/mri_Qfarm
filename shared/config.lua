return {
    AuthorizationManager = "admin",
    Debug = true,
    Interaction = "target", -- target, marker, zone,
    ShowMarker = true,
    ShowOSD = true,
    UseEmoteMenu = true,
    IconAnimation = "fade",
    FarmBoxWidth = 1, -- só mexa se souber o que está fazendo
    FarmBoxLength = 1, -- só mexa se souber o que está fazendo
    FarmBoxHeight = 1, -- só mexa se souber o que está fazendo
}
